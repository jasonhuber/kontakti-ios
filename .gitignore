# Xcode / SwiftUI
*.xcworkspace/xcuserdata/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM
build/

# React Native
node_modules/
.bundle/
ios/build/
android/build/
android/app/build/
android/local.properties
*.keystore
!debug.keystore
.gradle/
*.jks

# Env
.env
.env.local

# Secrets (API keys — never commit)
**/EnrichmentSecrets.swift

# macOS
.DS_Store

# Apple Push private key (NEVER commit)
AuthKey_*.p8
# Google OAuth client plist (NEVER commit)
client_*.apps.googleusercontent.com.plist
